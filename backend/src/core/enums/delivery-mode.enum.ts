/** How a tutoring session is delivered (Algorithm.md §2.4). */
export enum DeliveryMode {
  /** Remote / virtual session. */
  ONLINE = 'online',

  /** Physical, face-to-face session. */
  IN_PERSON = 'in-person',
}
