/** Tutor's teaching approach — part of the style vector (Algorithm.md §2.1). */
export enum TeachingStyle {
  /** Discussion-driven, question-and-answer format. */
  INTERACTIVE = 'interactive',

  /** Traditional one-way instruction format. */
  LECTURE = 'lecture',
}
