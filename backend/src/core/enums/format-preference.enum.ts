/** Session format — how many participants are expected. */
export enum FormatPreference {
  /** Private, single-student session. */
  ONE_ON_ONE = 'one-on-one',

  /** Shared session with multiple students. */
  GROUP = 'group',
}
