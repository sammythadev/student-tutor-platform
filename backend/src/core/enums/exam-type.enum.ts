/**
 * Nigerian secondary-school exam boards supported by the platform.
 * Single source of truth — seeds, harnesses, and defaults must use these values.
 */
export enum ExamType {
  WAEC = 'waec',
  NECO = 'neco',
  JAMB = 'jamb',
}
